## 1. Workbench route and navigation

- [x] 1.1 Register `/workbench` as an authenticated Vue Router route backed by `WorkbenchView.vue`.
- [x] 1.2 Add Workbench to the shared navigation list without changing `/dashboard` as the default authenticated home.
- [x] 1.3 Add zh-CN and en-US navigation labels for the Workbench entry.

## 2. Verification

- [x] 2.1 Add or update frontend unit tests proving `/workbench` is routed and present in resolved navigation.
- [x] 2.2 Run frontend type/test/build verification for the changed files.

## 3. Migration closure follow-up

- [x] 3.1 Record any confirmed lease subtype, overtime billing, discount/deposit/surplus/interest, or print/PDF depth gaps as separate follow-up OpenSpec changes instead of widening this route-only closure.
