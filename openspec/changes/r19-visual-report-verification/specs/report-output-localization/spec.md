## MODIFIED Requirements

### Requirement: Report outputs SHALL use localized operator-facing terminology
All operator-facing report outputs, including exported artifacts, SHALL use localized labels and terms appropriate to the user's locale.

#### Scenario: R19 localized tabular artifact is explicitly verified
- **WHEN** an operator exports the structured artifact associated with `R19`
- **THEN** verification SHALL confirm that exported header labels use accepted localized terminology for the visual shop analysis output fields
