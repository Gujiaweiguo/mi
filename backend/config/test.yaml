app:
  name: mi-backend
  environment: test

server:
  host: 0.0.0.0
  port: 8080
  read_timeout_seconds: 10
  write_timeout_seconds: 10

database:
  host: mysql
  port: 3306
  name: mi_test
  user: mi_test
  password: change-me
  ssl_mode: disable

auth:
  jwt_secret: change-me-test-secret
  token_expiry_seconds: 3600

log:
  level: info

storage:
  generated_documents_path: /app/generated-documents
  uploads_path: /app/uploads
  logs_path: /app/logs
